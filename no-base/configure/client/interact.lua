-- Docs: https://none-fivem.gitbook.io/nonem/resources/no-base/configuration/interact-target

Config.Interact = {
    -- AddEntryByModel = function(models, entries, options)

    -- end,
    -- AddEntryBySphereZone = function(coords, radius, entries, options)

    -- end
}
