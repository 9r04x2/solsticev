-- Docs: https://none-fivem.gitbook.io/nonem/resources/no-base/configuration/alerts

-- interface Indicator {
    -- text: string,
    -- icon: string,
    -- keys: string | string[]
-- }

Config.Alerts = {
    FontId = 4,
    Colors = {
        error = "#5A1A1A"
    },
    -- notification = function(text, error, icon, timeout)

    -- end,
    -- setIndicator = function(data) -- Indicator

    -- end,
    -- setIndicators = function(data) -- Indicator[]

    -- end,
    -- closeIndicator = function()
        
    -- end,
    -- closeIndicators = function()

    -- end
}