-- print("^2Jim^7-^2DJBooth ^7v^41^7.^43^7.^42 ^2by ^1Jimathy^7")

Config = {
	Lan = "en",
	Debug = false, -- Set to true to show target locations

	Core = "qb-core",
	Menu = "ox",
	Notify = "qb",

	Locations = {
		{ -- Vanilla Unicorn DJ Booth
			job = "unicorn", -- Set this to required job role 
			enableBooth = true, -- option to disable rather than deleting code
			DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
			radius = 50, -- The radius of the sound from the booth
			coords = vector3(120.5490, -1281.5754, 29.4805), -- Where the booth is located
		},
		{ -- Vanilla Unicorn DJ Booth
			job = "bahama", -- Set this to required job role
			enableBooth = true, -- option to disable rather than deleting code
			DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
			radius = 38, -- The radius of the sound from the booth
			coords = vector3(-1376.84, -607.71, 31.32), -- Where the booth is located
		},
		{ -- Vanilla Unicorn DJ Booth
			job = "uwucafe", -- Set this to required job role
			enableBooth = true, -- option to disable rather than deleting code
			DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
			radius = 25, -- The radius of the sound from the booth
			coords = vector3(-70.48, 821.86, 227.75), -- Where the booth is located
		},
		{ -- Vanilla Unicorn DJ Booth
		job = "mai10", -- Set this to required job role
		enableBooth = true, -- option to disable rather than deleting code
		DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
		radius = 25, -- The radius of the sound from the booth
		coords = vector3(-370.03, 204.23, 80.84), -- Where the booth is located
	},
		{ -- Vanilla Unicorn DJ Booth
		job = "skypub", -- Set this to required job role
		enableBooth = true, -- option to disable rather than deleting code
		DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
		radius = 25, -- The radius of the sound from the booth
		coords = vector3(-812.75, -716.77, 123.28), -- Where the booth is located
	},
		{ -- Vanilla Unicorn DJ Booth
		job = "iskelebar", -- Set this to required job role
		enableBooth = true, -- option to disable rather than deleting code
		DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
		radius = 25, -- The radius of the sound from the booth
		coords = vector3(-1144.50, -1700.95, 5.10), -- Where the booth is located
	   },
	   { -- iskele
	   job = "iskelebar", -- Set this to required job role
	   enableBooth = true, -- option to disable rather than deleting code
	   DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
	   radius = 25, -- The radius of the sound from the booth
	   coords = vector3(-1836.40, -1184.62, 13.59), -- Where the booth is located
	  },
		{ -- Vanilla Unicorn DJ Booth
			job = "tequila", -- Set this to required job role
			enableBooth = true, -- option to disable rather than deleting code
			DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
			radius = 30, -- The radius of the sound from the booth
			coords = vector3(-559.85, 281.58, 85.68), -- Where the booth is located
		},
	{ -- Vanilla Unicorn DJ Booth
		job = "japane", -- Set this to required job role
		enableBooth = true, -- option to disable rather than deleting code
		DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
		radius = 30, -- The radius of the sound from the booth
		coords = vector3(-1473.89, 165.66, 59.02), -- Where the booth is located
	},
	{ -- Mirror Park Cafe  DJ Booth
	job = "mprestaurant", -- Set this to required job role
	enableBooth = true, -- option to disable rather than deleting code
	DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
	radius = 30, -- The radius of the sound from the booth
	coords = vector3(-1297.20, -1037.25, 13.16), -- Where the booth is located
	},
	{ -- Mirror Park Cafe  DJ Booth
	job = "club77", -- Set this to required job role
	enableBooth = true, -- option to disable rather than deleting code
	DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
	radius = 30, -- The radius of the sound from the booth
	coords = vector3(247.22, -3187.56, 0.50), -- Where the booth is located
	},
	{ -- Vanilla Unicorn DJ Booth
		-- job = "public", -- Set this to required job role
		enableBooth = true, -- option to disable rather than deleting code
		DefaultVolume = 0.1, -- 0.01 is lowest, 1.0 is max
		radius = 30, -- The radius of the sound from the booth
		coords = vector3(-3010.53, 58.88, 12.1) -- Where the booth is located
	},
	},
}

Loc = {}